# 8-26 第三轮（真 LLM 复证）

对照第二轮（同工作区、真 glm-5.2、insider 停在 `executing_response` / 0 job）做全链路复证。**不再堆第一次远端全文。** 正文以本文件为准。

| 项 | 值 |
|---|---|
| 工作区 | `fix/close-report-memory-and-projections` @ `d8720ad7` + **未提交**脏树（约 136 文件：org_context / 量规 / RAG / B-1′ plumbing / F-1 bind 测试 / F-2 cmdline / F-3 分批 / S-2 Registry） |
| 相对 `origin/main` | `6609c79b`（8-17）。本分支领先 1 个已提交 commit；**不是 main** |
| 探查日期 | 2026-08-26 |
| Live 范围 | **只开 LLM**（`openai_compatible` / `glm-5.2`）。XDR 仍 Mock：`SOURCE_MODE=mock_xdr`、`DISPOSITION_MODE=mock_xdr`、`TOOL_MODE=mock`。未跑 CrowdStrike live 处置 |
| 密钥 | gitignored `.env.llm.audit`；**不入库、不写进本报告**。聊天里出现过的 key 视为已暴露，用完请在火山方舟轮换 |
| 立场 | fail-closed。一次 glm 金标 ≠ Mock CLOSED 合同。不改 LangGraph / `final_verdict=none` / CLOSED 门去涂绿 |

冻结（不当成「修掉才算过」）：ISSUE-328；`--require-closed` 合同；金标禁止手搓 `POST /events`、禁止靠 `APPROVAL_TIMEOUT` 收尾；写回恢复不得消耗 `replan_count`；#644 `AttackKBService.search_techniques` 直连旁路。

未 `merge` 落后 67 commit 的 `cursor/pre-release-audit-a07a`。

---

## 执行摘要

同一套 Demo 栈顺序跑官方三场景（`EVAL_REQUIRE_CLOSED=1`，`DYNAMIC_EVAL_MAX_WAIT_S=900`，脚本批准，不用 `APPROVAL_TIMEOUT`）：

| 场景 | 事件 | 墙钟 | `--require-closed` | 终态 | verdict / score | execute / jobs |
|------|------|------|---------------------|------|-----------------|----------------|
| `insider_data_exfiltration` | `evt-20240615-420918b5` | 901s 超时 | **失败** | `verifying` + `execution_substate=manual_resolution` | `possible_false_positive` / 83 | **有**：7 job `success`，轨迹含 `investigation:execute_plan` |
| `account_anomaly_fp` | `evt-20240615-628d3dba` | 180s | **通过** | `closed` | `false_positive` / 48 | 未走处置链（`close_as_fp` post_evidence） |
| `suspicious_domain_access` | `evt-20240615-a21c0948` | 260s | **通过** | `closed` | `possible_false_positive` / 49 | **有**：2 job `success` + `approval_plan_resume` terminal |

**B-1′ plumbing 本轮有实证改善**（上轮 0 job / 停在 `executing_response`；本轮 insider 与 domain 都进入 execute）。insider **仍未 CLOSED**：Verify 把 deferred disposition 虚动作标成 `need_manual_resolution`。这是 B-1 家族合同，**未改、不得写成已修**。

两次 glm CLOSED（FP / domain）**不是**产品合同，也不是「Mock `final_verdict=none` 路径已消失」。三场景 `replan_count=0`，动态重规划 **未 live 触发**。

知识库本轮显式 `make load-kb` 后金标期间非空（fresh volume 上 `knowledge_chunk=137`；`GET /knowledge` total=137）。insider/domain 的 RAG **有** `org_context_kb` citations，但 `org_context_matches` 仍为 0。#644 直连仍标冻结。

---

## 1. 怎么跑的

```bash
# host llm-smoke（sourced gitignored overlay）：非 silent mock
make llm-smoke
# probe status=ok, primary_model=glm-5.2, synthetic_chat_status=success

COMPOSE_BAKE=0 DOCKER_BUILDKIT=0 make down-demo && make up-demo
# -f infra/docker-compose.llm-audit.yml --force-recreate backend worker scheduler-*

make load-kb
DYNAMIC_EVAL_MAX_WAIT_S=900 EVAL_REQUIRE_CLOSED=1 EVAL_SCENARIO=<scenario> make eval-full-loop
```

顺序：`insider_data_exfiltration` → `account_anomaly_fp` → `suspicious_domain_access`。**未**跑 `eval-full-loop-matrix --fresh-volumes`。

复用上轮 volume 时 seed 被 watermark / behavior_observation hash 挡住；本轮金标前 **一次** `compose down -v` 清库（不是每场景重建）。金标期间停了 `scheduler-worker`，避免空轮询把 watermark 推到「现在」导致 2024 金标种子被跳过。

`GET /api/v1/health`（overlay 注入后）：`llm.status=ok`、`mode=openai_compatible`、`primary_model=glm-5.2`；`source_adapter` / `disposition_adapter` = `mock_xdr`；`scheduler-beat` Health=`healthy`，healthcheck 为 `/proc/1/cmdline` 含 `beat`（不再 `pgrep: not found`）。

---

## 2. 上轮条目对照

| 条目 | 上轮（第二轮） | 本轮 | 判定 |
|------|----------------|------|------|
| B-1 | Mock：`verifying` + `final_verdict=none` + `manual_resolution` | **未改合同**。insider 停在 `verifying` + `manual_resolution`，verdict 是 `possible_false_positive` 不是 `none` | **未修** |
| B-1′ | glm insider：`executing_response`、0 job、0 resume intent | 7 job success；`investigation:execute_plan`；`graph_resume_intent.approval_plan_resume=terminal`。insider 仍非 CLOSED | **plumbing 改善**；blocker 从「批准后不 execute」转为「execute 之后的 verify/disposition hold」 |
| F-1 | 观测 bind 已钉死，缺单测 | `up-demo` 可起；`test_compose_observability_binds.py` 2 passed | **复证已修** |
| F-2 | beat `pgrep: not found` / unhealthy | 容器 healthy；healthcheck 用 cmdline；单测绿 | **复证已修** |
| F-3 | 代码已分批；金标未 LOAD_KB、表 0 行 | `load-kb` 后表非空；`test_upsert_chunks_batches_above_embed_limit` passed | **复证已修**（默认金标仍不自动 LOAD_KB） |
| S-1 | `production_settings()` 单测绿 | 本轮 **未重跑** 该用例 | **未复证** |
| S-2 | Socket.IO `degraded` + `_WrappedReferencingError` | 全程 `socketio.status=ok`、`consecutive_failures=0`；相关单测绿。**未复现**上轮 ref 错 | **未复现**；单测绿 ≠ 上轮故障已在 live 证伪干净 |
| K-1 / #644 | `search_techniques` 直连，不在图内 RAG | 仍冻结；图内 RAG 本轮有 citations | **仍冻结** |
| K-2 重规划 vs 写回 | 单测符合合同；金标 `replan_count=0` | 单测 127 passed（含 never-replan）；三场景仍 `replan_count=0` | **合同单测成立**；**未 live 触发** |
| R-1 量规 | glm `llm_and_rule` / `valid`，非 `rule_only` | 三场景 risk 均为 `scoring_mode=llm_and_rule`、`llm_admissibility=valid` | **实证一次模型**，不是产品合同 |
| E-1 中文路径 / 墙钟 | `COMPOSE_BAKE=0`；默认 240s 不够 | 同；900s 对 FP/domain 够，insider 仍因终态不是 CLOSED | **环境限制仍在** |

---

## 3. 分级问题清单

每条：**级别** · **复现** · **路径** · **会不会误伤 `--require-closed`**。

### B-1 | verifying + manual_resolution（CLOSED 合同，未改）

- **级别：** blocker（plumbing `--require-closed`，在「会走 deferred disposition 虚动作」的路径上）
- **复现：** 本轮 `EVAL_REQUIRE_CLOSED=1 EVAL_SCENARIO=insider_data_exfiltration` → `evt-20240615-420918b5` 终态 `verifying`；`manual_hold.reason=verify_need_manual_resolution`；verification `overall_status=manual_resolution`，`blocked_writebacks=['act-b4a99e35']`（Deferred disposition-only virtual meta，`writeback_required=True` 且 `effect_status=skipped` / `deferred_pending_activation`）
- **路径：** Verify Phase / `terminal_disposition_resolver` / deferred disposition 虚动作。**未**改 `final_verdict=none` 门。上轮 Mock 是 `none`；本轮 glm 是 `possible_false_positive`。机制同类：分析师 hold，金标脚本批准处置 **不会** 自动写终端 disposition。
- **会不会误伤金标 CLOSED：** **会**（insider）。domain 本轮没有这条虚动作，所以能 `investigation:close`。
- **明确：** 不得把 B-1 写成已修。不得靠拉 `APPROVAL_TIMEOUT` 收尾。

### B-1′ | 批准后丢失续跑（上轮 blocker）

- **级别：** 上轮 blocker → **本轮 plumbing 改善（有 runtime 证据）**
- **复现对照：** 同一命令、同一 `evt-20240615-420918b5`。上轮停在 `executing_response`，`action_execution_job` 0 行，轨迹最后一跳 `ApprovalEngine / plan_fully_decided`。本轮：批准 7 条后出现 7 个 `success` job；轨迹 `waiting_approval → executing_response → verifying`（`investigation:execute_plan`）；`graph_resume_intent` 一行 `approval_plan_resume` / `terminal`。domain 同样有 `approval_plan_resume` + execute + CLOSED。
- **路径：** `graph_resume.py` deferred；`ApprovalEngine` 同租约追跑 / `approval_plan_resume`；`approval_wait_node` 已 executing 短接。单测：`test_resume_returns_deferred_when_lease_held`、`test_evaluate_plan_lease_deferred_is_not_ok_and_enqueues_wake`、`test_approval_wait_node_skips_waiting_substate_when_already_executing`、`test_approval_plan_resume_deferred_requeues`、`test_catchup_resumes_once_from_approval_wait_halt` **passed**。
- **会不会误伤金标 CLOSED：** 本轮 insider **不再**卡在「0 job」。仍会误伤的是 B-1 verify hold。一次 glm 成功续跑 ≠ 合同保证。

### F-1 | 观测 compose bind

- **级别：** 上轮 demo 起不来 → **本轮复证已修**
- **复现：** `COMPOSE_BAKE=0 DOCKER_BUILDKIT=0 make up-demo` 成功；`test_makefile_demo_sets_observability_config_dir` / `test_observability_compose_bind_sources_use_config_dir` passed
- **路径：** Makefile `OBSERVABILITY_CONFIG_DIR`；`infra/observability/docker-compose.observability.yml`
- **会不会误伤金标 CLOSED：** 修前会（栈起不来）。现为 compose 路径，不是 CLOSED 涂绿。

### F-2 | scheduler-beat healthcheck

- **级别：** **本轮复证已修**
- **复现：** `docker inspect …scheduler-beat` Health=`healthy`；Test=`python … /proc/1/cmdline` 含 `beat`；无 `pgrep`。`test_scheduler_beat_healthcheck_does_not_require_pgrep` passed
- **路径：** `infra/docker-compose.yml` `scheduler-beat.healthcheck`；`scripts/smoke_demo.sh`
- **会不会误伤金标 CLOSED：** 直接金标不依赖 beat health；会误伤 `smoke-demo` / `depends_on: service_healthy`。

### F-3 | upsert 分批 embed / LOAD_KB

- **级别：** **本轮复证已修**（加载路径）
- **复现：** `make load-kb` → attack 79 + fp 14 + history 20 + org_context 11（fresh volume 合计 137）；`GET /api/v1/knowledge` total=137。`TestUpsertChunks::test_upsert_chunks_batches_above_embed_limit` passed
- **副作用：** 该单测夹具连了本机 `localhost:5432`（即 Demo Postgres），跑完后 `knowledge_chunk` 被清到 3 行。金标 **已经** 在清库前跑完。
- **会不会误伤金标 CLOSED：** 默认金标不 `LOAD_KB`，**不直接**挡 CLOSED。

### S-2 | Socket.IO `$ref` / null `impact_assessment`

- **级别：** 上轮疑似 → **本轮未复现**；单测绿
- **复现：** 三场景期间 `GET /api/v1/health` 的 `socketio.status=ok`。`test_approval_required_null_impact_assessment_validates_offline`、`test_dispatch_ref_resolution_error_does_not_increment_failures` passed
- **疑似残留：** `last_success_at` 停在容器启动时刻（`12:26:17Z`），整场金标未刷新。未把它写成 S-2 已修或仍坏。
- **会不会误伤金标 CLOSED：** 本轮没有。

### N-1 | 空轮询把 ingestion watermark 推到「现在」

- **级别：** 疑似（运维 / 同栈多场景 seed）
- **复现：** 复用 volume 时 `source_checkpoint.updated_after` 变成当前墙钟，`seed_mock_xdr_and_ingest` 对 2024 金标对象 `accepted=0`。`_next_watermark` 在 `page.has_more=false` 时用 `page.server_time`
- **路径：** `backend/app/ingestion/source_ingester.py` `_next_watermark`；Demo `scheduler-worker` 60s 空转
- **会不会误伤金标 CLOSED：** 不直接。会让 **同栈第二场景 seed 失败**（本轮靠清 watermark + 停 ingestion worker 绕过）。不是 CLOSED 涂绿。

### N-2 | `GET /events` 被一条残缺 `creation_source_ref` 打成 500

- **级别：** 疑似
- **复现：** 上轮残留 `evt-277-*` 的 `creation_source_ref={"source_product":"mock_xdr"}` → list 500，金标脚本 `before_ids` 起不来
- **路径：** `event_service._security_event_from_row` / `SourceReference.model_validate`
- **会不会误伤金标 CLOSED：** 间接（脚本列事件失败）。清掉坏行后可跑。

### N-3 | 库非空但 `org_context_matches` 仍常为 0

- **级别：** 疑似（检索≠结构化匹配）
- **复现：** insider citations 15（org_context_kb 5 / fp 5 / history 5），`org_context_matches` 空；domain 同样 citations 15、matches 0；FP matches=2。`knowledge_query_plan` 顶层几乎只有 `playbook_kb`，`tenant_id=""`
- **路径：** RAG pipeline / `org_context_matcher`；图内 RAG **不是**全坏
- **会不会误伤金标 CLOSED：** 本轮 CLOSED 结果不由此决定。不要写成「检索全坏」。#644 仍冻结。

### N-4 | CLOSED 事件带 `redis_context_unavailable`

- **级别：** 疑似
- **复现：** FP / domain 已 `closed`，仍有 `redis_context_unavailable=true` 与 `state_transition_projection_degraded=side_effect_convergence:returned_degraded`
- **会不会误伤金标 CLOSED：** 本轮 `--require-closed` **仍接受**这两条 CLOSED（脚本未因 degraded_flags 失败）。

### R-1 | 风险量规（真模型抽查）

- **级别：** 实证，非合同
- **证据：** 三场景均为 `scoring_mode=llm_and_rule`、`llm_admissibility=valid`，**不是** `rule_only`。health `llm.audit`：窗口内 32 calls / 18 success，`last_status=llm_invalid_json`（真模型 JSON 失败会重试；失败耗尽才会 `rule_only`）
- **会不会误伤金标 CLOSED：** 本轮 CLOSED 卡点不在出分。

### E-1 | 本机环境

- **级别：** 环境限制
- **Docker：** 路径含 `副本` → 必须 `COMPOSE_BAKE=0 DOCKER_BUILDKIT=0`
- **墙钟：** 默认 `DYNAMIC_EVAL_MAX_WAIT_S=240` 不够 glm。本轮 900s：FP/domain CLOSED；insider 900s 仍是 `verifying`，不是 timeout 不够。

---

## 4. 三场景探针（隐秘路径，不只看脚本绿）

### insider_data_exfiltration — `--require-closed` 失败

- 终态：`verifying`（不是 `executing_response`）
- `final_verdict=possible_false_positive`，`risk_score=83`，`replan_count=0`
- 脚本批准 7 条；`action_execution_job` **7 success**；`status_trace` 含 `investigation:execute_plan`
- `graph_resume_intent`：`approval_plan_resume` / `terminal`（上轮 0 行）
- Celery `inspect active` 空（结束后）
- 写回恢复 **未**进 `REPLANNING`，未消耗 `replan_count`
- Socket.IO health：ok（无 `_WrappedReferencingError`）
- RAG：`degraded=false`，citations 非空，`org_context_matches` 空
- 卡点：deferred disposition 虚动作 → `need_manual_resolution`

### account_anomaly_fp — CLOSED

- `closed` @ 180s；`false_positive` / 48；`close_as_fp post_evidence`
- **无** scripted approve 日志、0 job、0 resume intent（未进入 waiting_approval）
- `generate_report` L0 success
- `org_context_matches=2`；citations fp/history/org 均有
- `replan_count=0`；`scoring_mode=llm_and_rule`
- **不**把这次 FP 短路写成「B-1′ 已修」——这条路径根本没批准/execute

### suspicious_domain_access — CLOSED

- `closed` @ 260s；`possible_false_positive` / 49
- 脚本批准 **1** 条；2 job success；`investigation:execute_plan` → `investigation:report` → `investigation:close`
- `approval_plan_resume` terminal
- citations 15，`org_context_matches=0`
- `replan_count=0`
- 一次 glm CLOSED ≠ 合同。与 insider 对比：本场景没有把 deferred disposition 虚动作留到 verify hold

---

## 5. 单测（不替代金标）

```text
127 passed（24.9s）
test_replan_loop.py
test_writeback_recovery.py
test_graph_resume.py
test_approval_wait_node_skips_waiting_substate_when_already_executing
test_evaluate_plan_lease_deferred_is_not_ok_and_enqueues_wake
test_approval_plan_resume_deferred_requeues
test_approval_required_null_impact_assessment_validates_offline
test_dispatch_ref_resolution_error_does_not_increment_failures
test_scheduler_beat_healthcheck_does_not_require_pgrep
test_compose_observability_binds.py
TestUpsertChunks::test_upsert_chunks_batches_above_embed_limit
```

金标三场景 `replan_count=0`：**标为未 live 触发**。写回恢复单测仍断言不进 REPLANNING、不消耗 `replan_count`。

---

## 6. 本轮没跑 / 没证伪

- CrowdStrike / 任何 live XDR 处置（`ALLOW_LIVE_SIDE_EFFECTS` 仍 false）
- `eval-full-loop-matrix --fresh-volumes`（按计划用同一 Demo 栈顺序三场景代替）
- S-1 `test_from_rag_input_requires_tenant_in_production` **未重跑**
- K-3 FP 全环不短路的独立 ISSUE-114 套件（本轮 FP 场景走了 `close_as_fp`，与「不短路」产品叙述可能冲突，**未**当本轮 blocker 展开）
- K-4 ReportAgent 180s 下限（HEAD `d8720ad7` 已有；本轮未单独压 report timeout）
- K-5 ISSUE-328
- `smoke-demo` 全脚本（只复证了 beat healthcheck 形态 + Demo 健康）
- 上轮 `final_verdict=none` 的 Mock 金标 **未**再跑一遍

---

## 7. 明确不做

- 不改 LangGraph / CLOSED / Outbox / `final_verdict=none` 让 insider 涂绿
- 不把 FP/domain 两次 glm CLOSED 写成 Mock 合同或「B-1 已消失」
- 不把 API key 或 Authorization 写入 git / 本报告 / canvas
- 不 merge 落后 67 commit 的审计分支
