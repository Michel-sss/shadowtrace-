# ShadowTrace 技术创新文档

> 赛题：XH-202614《AI+安全大模型平台的智能体研究》（揭榜单位：深信服科技、张家口默然科技）。  
> 权威来源：仓库代码与 [`ShadowTrace 项目总览.md`](ShadowTrace%20项目总览.md)。本文区分**机制创新**、**工程落地**与**明确不声称**，不把 LangGraph + RAG 写成算法突破。  
> 可演示闭环以 **Mock XDR + 真 LLM** 为准。Sangfor Adapter 合同已落地，**Cutover-Ready ≠ 真机验证**，不得声称已对接生产 XDR。

---

## 1. 定位

ShadowTrace 是独立部署的**通用多 Agent 安全运营智能体**：接收告警后完成分诊、取证、知识增强、关系图、风险评分、分级处置、独立验证与报告，并以**强证据**进入 `CLOSED`。

与厂商 XDR、具体大模型解耦，三个可替换边界：

| 边界 | 职责 | 不得越权 |
|---|---|---|
| `SourceAdapter` | 只读接入事件 / 告警 / 资产 / 日志 | 不写回、不执行动作 |
| `ToolProvider` | 查询取证与实体处置（`DIRECT_TOOL`） | 失败不得静默回退 Mock 后返回成功 |
| `DispositionAdapter` | 获批处置与最小结果写回来源 | 分析正文、报告、Prompt、决策轨迹永不写回 |

LLM 一律走 `LLMProvider`（`mock` / `openai_compatible` / `custom`）。业务 Agent 不绑定厂商 SDK。每个 Action 只能有一个 `execution_owner`：`XDR_MANAGED` 或 `DIRECT_TOOL`，禁止双下发。

覆盖 8 类 `EventType`：账号异常、主机失陷、数据外泄、内部威胁、恶意进程、可疑域名、横向移动、其他。调查生命周期 14 态 `EventStatus`，与判定标签 `FinalVerdict` 正交。

---

## 2. 赛题对标

| 赛题要求 | 本系统对应 | 缺口与边界 |
|---|---|---|
| 任务1 · 安全运营智能体（告警研判等高频场景） | 专职 Agent 流水线；8 类事件自动分诊→取证→研判→处置；漏洞排查对应 `query_vuln_info` | 邮件降噪、会话跟踪为赛题举例，本系统主路径是「告警→闭环处置」 |
| 任务2 · 安全知识库 + RAG + 工具扩展 | ATT&CK / 剧本 / 案例库 + pgvector 混合检索；`ToolProvider` 可插拔（查询 / 处置 / 验证 / 回滚） | 评测默认 `EMBEDDING_MODE=mock`；远程向量是另一条 demo 轨道 |
| 挑战任务 · SuperAgent 闭环 | LangGraph 状态机；多源证据；处置+验证协同；`REPLANNING`（≤3）；两阶段验证 + 写回 | L0/L1 自动、L2+ 人工；默认金路径不接生产防火墙 |
| 展示推理过程 | `decision_trace` 时间线 + 结构化 `decision_basis` | **原始 CoT 永不落库**，只存 SHA256 / `[NOT_RETAINED]`。材料请写「可解释决策依据」，不要写「展示原始思维链」 |

---

## 3. 关键技术

```mermaid
flowchart LR
  ingest[接入归一化] --> orch[LangGraph编排]
  orch --> agents[专职Agent]
  agents --> rag[混合RAG与C-RRF]
  agents --> graph[实体图与路径]
  orch --> exec[审批执行验证]
  exec --> closed[强证据CLOSED]
```

主链（`backend/app/orchestration/workflow_graph.py`）：

`triage → planner → evidence → fp_adjudication → rag∥graph → risk → response → approval → execute → verify → report → close`

另有 `approval_wait`、`replan`、`writeback_recovery`、`halt`、`manual_hold`、`begin_disposition_only`。RAG 与 Graph **只依赖证据、互不依赖**，在同一 superstep 并行；Risk 前汇合。取证后误报裁决是**规则阶梯**，不是一段 FP LLM。

### 3.1 多智能体编排与可解释决策

编排可信、决策可解释、行为可审计。

- **LangGraph 状态机**：约 19 个命名节点；checkpoint + 调查租约，审批/人工挂起可断点续跑。专职实现是 **11 个 Agent 类**（Super / Planner / Triage / Evidence / Graph / RAG / Risk / Response / Verify / Report / Memory）+ `ToolExecutor`（`AgentName` 中的 `tool_agent` 落在执行器，不是第十二个 Agent 类）。
- **工作记忆字段归属**（`working_memory.py`）：`FIELD_OWNERSHIP` 与 `EventContext` 字段启动时双向校验；非 owner 写入抛 `working_memory_unauthorized_write`；CAS 防并发污染。
- **收敛与预算**（`convergence_guard.py` / `workflow.py`）：80 步 / 30 次 LLM / 3 次重复工具 / 2 次振荡，首个命中强制收敛。系统 100 万 token、单事件 10 万（按严重度 0.5–2.0 倍）、单事件成本 5 美元、单 Agent 2 万 token。超限形成「预算耗尽」报告，不伪造成功。重规划上限 `MAX_REPLAN_COUNT=3`。
- **四维输出护栏**（`core/guardrails.py`）：schema / grounding / 处置目标须在实体集内 / sanitization。出站处置 fail-closed。
- **可解释但不泄密**：`decision_trace_service.py` 合成 Agent / 工具 / LLM / 状态 / 审批 / 动作 / 处置 / 写回时间线。Agent 强制产出 `decision_basis`（输入摘要、证据引用、规则、结论、置信度）。`agent_trace_service.py` 对 CoT 与原始 payload 只存哈希，键标 `[NOT_RETAINED]`。

### 3.2 知识增强研判（RAG + C-RRF）

检索可控、证据可溯、知识可信。

- **流水线**（`rag/pipeline.py`）：查询构造 → 混合检索（pgvector 余弦 + PostgreSQL 全文）→ 融合 → 重排 → 引用。不引入独立向量库。
- **C-RRF**（`rag/constraint_rrf.py`）：vanilla RRF（Cormack 等，k=60）之上增加符号排序 `L_C`。`L_C` 按组织 **allow** 约束重叠给候选加票；`person_status` / `data_handling` / `security_product` 等 deny 类**不进**约束集 `H`，不能抬块。恒等条件：

  `H = ∅` 或 `L_C = ∅` ⇒ C-RRF ≡ RRF；否则 RRF(L₁…Lₘ, L_C)。

  `L_C` 只是融合投票人，不伪造 `OrgContextMatch`，不跳过 `attack_kb`。
- **知识库种子**（`data/knowledge/`，落笔时计数）：ATT&CK **79** 条技术 / **14** 战术（v15.1）；SOAR 剧本 **13**；误报案例 **14**；历史案例 **25**。权威知识走 STIX/JSON release（stage → 校验 → advisory-lock 原子激活）；生成式记忆经人工 review / promote / reject。
- **实体约束检索**：调查实体进入 keyword / RRF 通道（含 Account+Host AND 等口径），与 C-RRF 的组织 allow 约束分开，避免「没恶意」被正文叙述误抬。

### 3.3 实体关系图与攻击路径

把多源告警沉淀成有 ID、有边、有时间序的图；**主路径在 PostgreSQL**。

- 六类实体：账号 / 主机 / IP / 域名 / 进程 / 文件。
- 八种有向关系（`GraphRelationType`）：`logged_in_from` / `logged_in_to` / `executed` / `accessed` / `connected_to` / `resolved` / `requested` / `uploaded_to`。节点/边 ID 由内容哈希派生，重复建图幂等。
- 攻击路径：时间单调、跳数上限；跨事件横向移动以共享实体为线索。Neo4j 为可选 profile，**不是** P0 硬依赖。同步过滤集 `_RELATION_TYPES` 与上述 8 种枚举**不完全一致**，不得声称「8 种关系全部镜像到 Neo4j」。
- STIX 2.1 导出用于 ATT&CK 语料互通：attack-pattern ID 由 UUIDv5 派生，release 以 SHA256 内容哈希校验。

### 3.4 风险评分与误报资格

一致的风险分 + 防过信 + 误报关闭要授权。

- **六维加权**（默认 `FACTOR_WEIGHTS`）：资产影响 0.25、行为异常 0.20、证据置信度 0.20、攻击阶段 0.15、数据敏感度 0.10、威胁情报 0.10；可按 `EventType` 换曲线。
- **规则 + LLM 量规**：规则引擎先打分；LLM **不得直接输出 0–100**，只选情境带，由 `risk_rubric.land_factor_score` 把规则分落到带内。LLM 不可用 / 不可采纳时退化为纯规则，并标降级。证据稀疏时置信度封顶 0.35（只降不升）；温度默认 1.2 压低过信。
- **四态判定**（`VerdictResolver`）：`confirmed_threat` / `false_positive` / `possible_false_positive` / `none`。`close_as_fp` 不被高风险分覆盖。
- **PostEvidenceFpAdjudicator 是规则资格阶梯**（`fp_adjudication_service.py`）：要授权证据、时间窗、范围匹配，且无恶意冲突。**不是**「再跑一段误报 LLM」。没有恶意证据本身不能关误报。

### 3.5 处置闭环与可靠写回

执行 → 独立验证 → 强证据 CLOSED → 可回滚补偿；本地成功不得冒充外部已回写。

- **L0–L5**：L0/L1 自动批准；**L2 及以上一律人工**（fail-closed，禁止审批失败自动放行）。30 分钟超时拒绝。
- **Outbox**：命令白名单装配 → 投递前 TOCTOU 复审 → 行锁与序号幂等 → `READY → LEASED → DELIVERED`。`accepted ≠ confirmed`：接收不代表成功。
- **两阶段验证**：先核验 IMMEDIATE 动作效果，再激活 deferred 终态 `EVENT_STATUS_UPDATE` 并核验写回。
- **CLOSED 强证据门**（`workflow.py`）：`CLOSED_TERMINAL_STRONG_CONFIRMATION_EVIDENCE = {readback_verified, manual_confirmed}`。`adapter_acknowledged` / `status_queried` 不能过门。非 mock 还要求回执非 `simulated`。`writeback_required`（业务义务）与 `writeback_readiness`（能否提交）正交；能力默认 UNKNOWN，无正式文档确认不得 READY。
- **回滚**：回滚 ≠ 删除；独立 rollback 动作验证通过后才把原动作 CAS 为 `ROLLED_BACK`。`unknown` 禁止盲重试。

实时通道：Redis Pub/Sub → Socket.IO，信封类型 **18** 种（`SOCKET_MESSAGE_TYPES`，含 `event_type_rewritten`、`writeback_readback_failed` 等加固类型）。

---

## 4. 创新点分层

创新任务完成情况以总览为准：**31 / 34**。不计入的 3 项见第 4.3 节。

### 4.1 A 档 · 机制创新（相对「套一层 Agent + RAG」可指认）

| 点 | 新在哪 | 不是什么 | 代码锚点 |
|---|---|---|---|
| 三边界解耦 | 读、执行、写回分契约；分析永不写回 XDR | 不是「接了某个厂商 SDK 就算闭环」 | `adapters/source`、`providers/tools`、`adapters/disposition`；`OutboundDispositionGuard` |
| 字段归属工作记忆 | 上下文字段唯一受信 writer，越权即缺陷 | 不是共享 dict 或「大家都能改 context」 | `FIELD_OWNERSHIP` |
| 强证据 CLOSED | 写回 CONFIRMED 且确认等级够；Mock 必须标 `simulated=true` | 不是「本地状态 CLOSED 即外部已处置」 | `validate_closed_gate` |
| C-RRF 恒等设计 | 组织 allow 约束作额外投票；空 H 退回标准 RRF，行为可证明 | 不是换了一个黑盒重排模型 | `constraint_rrf.py` |
| 误报关闭资格制 | 授权 + 窗口 + 范围；无恶意不足以关单 | 不是相似度分数或「LLM 说是误报」 | `PostEvidenceFpAdjudicator` |
| 可解释且不留 CoT | 决策依据结构化可回放；原始思维链只哈希 | 不是把模型内心独白给评委看 | `agent_trace_service.py` |
| 单一 execution_owner | 禁止 Adapter 与 Direct Tool 砍同一刀两次 | 不是工具目录更长 | Action 上的 `execution_owner` |

### 4.2 B 档 · 工程落地（完成赛题，不作论文级算法突破）

这些是把闭环做全、可回归，属于交付能力而不是新理论：

- 混合检索（向量 + 全文 + RRF/重排）与引用追踪。
- L0–L5 分级审批、影响评估、Outbox 租约幂等。
- 只读 ReAct 补证（`REACT_ENABLED` **默认 false**；开启后两次拒绝即停，失败回退固定计划，禁止在 ReAct 里创建处置）。
- 攻击故事线、实体图 UI、15 章报告。
- 记忆沉淀与知识审核页。
- 金路径评测：`make demo-full-loop`；严格门 `EVAL_REQUIRE_CLOSED=1 make eval-full-loop`。Demo 默认 3 条 `GOLD_SCENARIOS`；8 类 EventType 另有 `eval-eventtype-8` 套件，二者不要混称为「已有 7 条金路径」。

### 4.3 C 档 · 明确不声称

| 不写进「已完成创新」 | 事实 |
|---|---|
| 已对接生产深信服 XDR / 安全 GPT | Sangfor REST 合同在 `adapters/sangfor` 与 `contracts/vendor/sangfor_xdr`；无现场凭证与库存不得用 Mock 绿冒充 live |
| ISSUE-031 上下文压缩 | 有单测与钩子，**生产 `deps.py` 未注入**，主链仍走截断 |
| ISSUE-061 Saga 回滚补偿 | `RollbackService.compensate` 可单测，**未接入 LangGraph / CLOSED / 前端** |
| ISSUE-089 三幕 `make demo` 讲解脚本 | 方案点名脚本不存在；官方入口是 `demo-full-loop` / `eval-full-loop` |
| 8 种关系全部进入 Neo4j | PostgreSQL 主图是 8 种；Neo4j 同步集合不一致 |
| 默认自动 ReAct 调查 | 默认关闭 |
| 用检索优化「让调查快百分之几十」 | 墙钟由真 LLM 主导；C-RRF / 并行节点不改变这一事实 |

---

## 5. 与常见方案的差别（给评委的一页）

| 常见做法 | ShadowTrace 的约束 |
|---|---|
| 聊天机器人调几个安全 API | 14 态编排 + 字段归属 + 收敛护栏，产物有唯一 writer |
| 单一大模型端到端研判 | 规则/量规可降级；评分与误报关闭都有非 LLM 路径 |
| SIEM 出工单、SOAR 跑死剧本 | 计划按能力清单生成；无写能力则 `writeback_unsupported`，不能把 required 改成「无需回写」 |
| 本地 DB 改状态当作已封禁 | Outbox + 回读；弱证据不能 CLOSED |
| 把 Prompt 全文当审计 | 只留结构化依据与哈希，满足审计又避免泄密 |

---

## 6. 可演示入口

默认栈：**真 LLM + 真 Agent / 审批 / 执行器 + Mock XDR**（`SOURCE_MODE=mock_xdr`，`DISPOSITION_MODE=mock_xdr`）。没有现场防火墙也必须能演示 CLOSED。

```bash
make up-demo && make demo-full-loop
# 严格写回门：
make up-demo && make bootstrap-demo-full-loop
EVAL_REQUIRE_CLOSED=1 make eval-full-loop
```

评委可看：事件详情决策轨迹、`GET /api/v1/events/{id}/decision-trace`、关系图、报告 ATT&CK 引用、审批（L2+）、写回徽章（Mock 回读与 live 已验证不得画成同级绿）。

`bootstrap-demo` 默认不含 response，**不是** CLOSED 金路径。

---

## 7. 数字速查（以代码为准）

| 项 | 值 |
|---|---|
| EventType | 8 |
| EventStatus | 14 |
| LangGraph 命名节点 | 19（含 halt / hold / recovery 等） |
| Agent 类 | 11 + `ToolExecutor` |
| Socket 信封 | 18 |
| ATT&CK 种子 | 79 技术 / 14 战术 / v15.1 |
| 剧本 / 误报案 / 历史案 | 13 / 14 / 25 |
| 创新任务 | 31/34（缺口 031、061、089） |
| CLOSED 强证据 | `readback_verified`、`manual_confirmed` |

---

## 8. 材料组注意

1. 写「可解释决策依据」，不要写「展示原始 CoT」。  
2. 写「Mock 契约闭环已跑通」；写 live 必须加「未做真机验证」。  
3. 误报能力请写资格制与案例库，不要写「误报大模型」。  
4. RAG 亮点优先写 C-RRF 恒等与引用可溯，不要写「自研向量数据库」。  
5. 金路径条数：Demo 3 条，EventType 套件 8 条，不要发明「7 条」。

*本文不修改评测默认、不修改 `GOLD_SCENARIOS`。需要贴图或深挖时按各节路径打开源码即可。*
