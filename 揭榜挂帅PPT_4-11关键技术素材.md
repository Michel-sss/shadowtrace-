# ShadowTrace 揭榜挂帅 PPT 技术素材（对应第 4–11 页）

> 供 khw 制作 PPT 使用。均来自仓库实际代码 + 上游 issue/PR 记录（`.planning/` 审计、commit 记录），附文件路径与 issue 编号可溯源。
> 第 12–17 页（功能展示 / 效果 / 性能测试）暂不覆盖，另行准备。

---

## ★ 赛题对标速查（XH-202614 评分核心，建议放 PPT 最前面一页）

赛题：**《AI+安全大模型平台的智能体研究》**（项目编号 XH-202614；揭榜单位：深信服科技、张家口默然科技）。

| 赛题要求 | ShadowTrace 对应实现 | 素材页码 |
|---|---|---|
| **任务1 · 构建安全运营智能体**：针对告警自动研判、邮件降噪、漏洞排查、会话跟踪等高频场景构建 Agent | 12 个专职 Agent 流水线；8 类事件自动分诊→取证→研判→处置；漏洞排查对应 `query_vuln_info`；告警研判即 P0 主链路（注：邮件降噪/会话跟踪属赛题举例场景，本系统聚焦"告警→闭环处置"，缺口可后续用工具扩展补齐） | 第 4、5、7 页 |
| **任务2 · 专业安全知识库增强 + 工具扩展**：ATT&CK 战术图谱、0day 原理等知识库 + RAG 知识增强 + 工具扩展 | 78 条 ATT&CK 技术（v15.1）/ 14 战术 + 13 SOAR 剧本 + 案例库；pgvector + 全文双路混合检索 RAG 流水线；ToolProvider 可插拔工具扩展（9 查询/13 处置/9 验证/6 回滚） | 第 5、8 页 |
| **挑战任务 · SuperAgent 闭环**：多源数据综合分析 + 多工具协同（防火墙/EDR）+ 持续观测、重规划（ReAct）+ 从发现威胁到闭环处置全自动化 + 减少人工干预 | LangGraph 19 节点状态机编排；8 类证据源综合分析；处置工具 + 验证工具协同；REPLANNING 重规划（ReAct）+ 两阶段验证 + 写回闭环；**L0/L1 自动审批、L2+ 人工兜底**显著减少人工干预 | 第 7、9、10、11 页 |
| **思维链 CoT 展示**：复杂事件处理中展示推理过程（例：账号异常→定位终端→进程→定位账号→判定攻击） | 用 **decision_trace 时间线 + 结构化 decision_basis**（证据引用/规则/结论/置信度）呈现"可解释的决策依据"，见下方 ⚠️ 提醒 | 第 7 页 |

> ⚠️ **重要提醒（CoT 处理，团队务必读）**：赛题要求"展示思维链推理过程"，但 ShadowTrace 出于隐私与审计考虑，**原始思维链（CoT）永不落库**，只存 SHA256 引用（`agent_trace_service.py`）。PPT 建议表述为"**可解释的决策依据**"——用 decision_trace 还原每一步推理（分诊依据→证据→ATT&CK 映射→评分→处置理由），既满足"展示推理过程"的评分点，又比原始思维链更可审计、不泄密，可反包装成**隐私合规亮点**。不要直接写"展示原始思维链"。

---

## 第 4 页 · 总体架构

**一句话**：ShadowTrace 是独立部署的**通用多 Agent 安全运营智能体系统**，与 XDR、大模型、厂商均解耦。

**三层可替换边界（核心架构思想）**
- `SourceAdapter`：只读接入 XDR 数据（事件/告警/资产/日志），**来源可读≠可写**。
- `ToolProvider`：执行防火墙/EDR 等处置动作（query / response / verification / rollback 四类工具）。
- `DispositionAdapter`：把获批的处置动作与最小执行结果同步写回来源 XDR。

**技术栈**
- 后端 FastAPI（Python 3.11）+ 前端 React + PostgreSQL（pgvector 向量扩展）+ Redis + Celery worker。
- 编排引擎 LangGraph（`langgraph>=0.2.0` + checkpoint，支持断点续跑）。
- 大模型统一走 `LLMProvider`（mock / openai_compatible / custom 三种模式），业务代码不绑定厂商。

**Agent 体系（12 个专职 Agent）**
SuperAgent（编排）→ TriageAgent（分诊）/ PlannerAgent（计划）/ EvidenceAgent（取证）/ GraphAgent（关系图）/ RAGAgent（知识增强）/ RiskAgent（评分）/ ResponseAgent（处置方案）/ VerifyAgent（验证）/ ReportAgent（报告）/ MemoryAgent（沉淀）/ ToolAgent（工具执行）。

**解耦原则**：XDR 是可替换数据源；开发期走 Mock 契约，真实环境按三层边界替换，切换时不改 Agent。

**分层架构图（khw 可直接照着画成 PPT 图）**

```text
┌─ 交互层 ────────────────────────────────────────────────┐
│   React 前端 · REST /api/v1 · Socket.IO 16 类实时事件      │
└──────────────┬───────────────────────────────────────────┘
┌─ 编排层 ──────┴──────────────────────────────────────────┐
│   SuperAgent · LangGraph 19 节点状态机 · 重规划闭环(ReAct) │
│   Planner→Triage→Evidence→Graph→RAG→Risk→Response→      │
│   Approval→Execute→Verify→Report→Close                  │
└──────────────┬───────────────────────────────────────────┘
┌─ 智能体层 ────┴──────────────────────────────────────────┐
│   12 个专职 Agent（分诊/取证/图谱/知识/评分/处置/验证/报告）│
└──────────────┬───────────────────────────────────────────┘
┌─ 工具层 ──────┴──────────────────────────────────────────┐
│   ToolProvider：9 查询 · 13 处置 · 9 验证 · 6 回滚         │
└──────────────┬───────────────────────────────────────────┘
┌─ 接入/写回层 ─┴──────────────────────────────────────────┐
│   SourceAdapter（只读接入）│ DispositionAdapter（写回）    │
└──────────────┬───────────────────────────────────────────┘
┌─ 存储/知识层 ─┴──────────────────────────────────────────┐
│   PostgreSQL(pgvector) · Redis · ATT&CK/剧本/案例知识库   │
└──────────────────────────────────────────────────────────┘
```

---

## 第 5 页 · 基础功能全览

**覆盖 8 类安全事件**：账号异常 / 主机失陷 / 数据外泄 / 内部威胁 / 恶意进程 / 可疑域名 / 横向移动 / 其他（`EventType`，`models/enums.py:172-182`），可扩展。

**P0 主链路（完整闭环 7 步）**
数据接入 → 分诊 → 证据采集 → 风险评分 → 处置建议 → 处置执行·写回·验证 → 报告输出。

**功能清单全览（可做 PPT 一页总览表）**

| 能力 | 规模 | 一句话作用 |
|---|---|---|
| 事件类型 | 8 类 | 覆盖主流安全事件，按类型匹配不同的调查模板 |
| Agent | 12 个专职 Agent | 串成一条自动调查流水线，无需人工逐环节操作 |
| 查询工具 | 9 类 | 只读取证，产出证据 |
| 处置工具 | 13 类 | 按 L0–L5 分级执行封禁/隔离/杀毒/改密等动作 |
| 验证工具 | 9 类 | 处置后回查是否真的生效 |
| 回滚工具 | 6 类 | 处置错了能撤销，回滚也要独立验证 |
| REST API | 六大块 | 前端页面和自动化脚本的统一调用入口 |
| 实时通道 | Socket.IO 16 类事件 | 前端实时刷新状态/进度/审批/写回 |
| 调查状态机 | 14 态 | 每起事件的编排生命周期，全程可追溯 |
| 处置分级 | L0–L5 六级 | 影响越小越自动，影响越大越要人批 |
| 判定标签 | FinalVerdict 四态 | 最终结论：确认威胁/误报/疑似误报/未判定 |
| 报告 | 15 章节 | 自动生成结构化调查报告，可直接输出 |

**各功能展开到具体分类（PPT 第二页可列明细）**

**① 事件类型 8 类**（`enums.py:172-182`）
`account_anomaly 账号异常`、`host_compromise 主机失陷`、`data_exfiltration 数据外泄`、`insider_threat 内部威胁`、`malicious_process 恶意进程`、`suspicious_domain 可疑域名访问`、`lateral_movement 横向移动`、`other 其他/未分类`。
→ 事件类型决定分诊模板、取证计划、处置预案的匹配方向。

**② 12 个专职 Agent**（按调查流水线顺序）
`SuperAgent 编排中枢 → TriageAgent 分诊 → PlannerAgent 出执行计划 → EvidenceAgent 证据采集 → GraphAgent 实体关系图/攻击路径 → RAGAgent 知识增强研判 → RiskAgent 风险评分 → ResponseAgent 处置方案 → VerifyAgent 处置验证 → ReportAgent 报告生成 → MemoryAgent 知识沉淀 → ToolAgent 工具执行统一入口`。

**③ 查询工具 9 类**（只读取证，产出证据）
`query_account_login 账号登录`、`query_edr_process 终端进程`、`query_file_access 文件访问`、`query_network_flow 网络流量`、`query_dns DNS 解析`、`query_asset_info 资产信息`、`query_vuln_info 漏洞信息`、`query_threat_intel 威胁情报`、`query_history_cases 历史案例`。

**④ 处置工具 13 类**（按 L0–L5 分级执行，产生外部副作用）
`block_ip 封禁IP`、`block_domain 封禁域名`、`isolate_host 隔离主机`、`quarantine_file 隔离文件`、`block_process 阻断进程`、`scan_host_for_virus 主机杀毒`、`disable_account 禁用账号`、`force_logout 强制下线`、`reset_password 重置密码`、`revoke_token 吊销令牌`、`create_ticket 建工单`、`notify_security_team 通知安全团队`、`update_source_event_disposition 事件状态回写`（终态写回 XDR 的唯一入口）。

**⑤ 验证工具 9 类**（处置后回查是否生效）
`check_ip_block_status 封禁状态`、`check_domain_block_status 域名封禁状态`、`check_host_isolation_status 隔离状态`、`check_file_quarantine_status 隔离文件状态`、`check_process_block_status 进程阻断状态`、`check_virus_scan_status 查杀状态`、`check_account_status 账号状态`、`check_new_alerts 新告警`、`check_traffic_drop 流量下降`。

**⑥ 回滚工具 6 类**（误处置撤销）
`unblock_ip 解封IP`、`unblock_domain 解封域名`、`restore_account 恢复账号`、`cancel_host_isolation 解除主机隔离`、`restore_file 恢复文件`、`close_false_positive_ticket 关闭误报关单`。

**⑦ REST API 六大块**（统一 `/api/v1` 前缀）
`事件类`（创建/查询/调查/关闭/报告/轨迹/审计/图/决策轨迹）、`审批裁决类`（approve / reject / resolve-unknown）、`数据接入类`（source-records / connectors / disposition-source）、`处置写回类`（dispositions / writebacks / retry / resolve）、`知识类`（knowledge / knowledge-reviews）、`平台类`（execution-jobs / tasks / tools / health / stats / search）。

**⑧ 实时通道 16 类事件**（Socket.IO，前端实时刷新）
`event_created 事件创建`、`state_change 状态变化`、`agent_progress / agent_completed / agent_failed Agent 进度与结果`、`tool_call_started / tool_call_completed 工具调用`、`approval_required / approval_updated 审批请求与更新`、`action_executed / action_verified 动作执行与验证`、`risk_updated 风险更新`、`report_generated 报告生成`、`final_verdict_updated 判定更新`、`disposition_submitted 处置提交`、`writeback_updated 写回更新`。

**⑨ 调查状态机 14 态**（事件编排生命周期，`enums.py:14-36`）
`NEW 新建 → TRIAGING 分诊 → COLLECTING_EVIDENCE 取证 → ANALYZING 分析 → SCORING 评分 → PLANNING_RESPONSE 出方案 → WAITING_APPROVAL 待审批 → EXECUTING_RESPONSE 执行 → VERIFYING 验证 →（不满意回）REPLANNING 重规划` → 终态 `CONTAINED 已遏制 / FAILED 失败` → `REPORTING 出报告 → CLOSED 关闭`。

**⑩ 处置分级 L0–L5**（按影响半径分级）
`L0 只读验证`（check_* 验证工具，无副作用）/ `L1 通知与建单`（notify_security_team、create_ticket）/ `L2 网络封禁`（封 IP、封域名）/ `L3 主机与文件处置`（隔离主机、隔离文件、阻断进程、杀毒、禁用账号、强制下线）/ `L4 凭据处置`（改密、吊销令牌）/ `L5 保留`。
→ **L0/L1 自动批准，L2 及以上一律人工审批**（ISSUE-147 收紧，杜绝 fail-open），30 分钟超时自动拒绝。

**⑪ 判定标签 FinalVerdict 四态**
`confirmed_threat 确认威胁 / false_positive 误报 / possible_false_positive 疑似误报 / none 未判定`，独立于事件流程状态。

**⑫ 报告 15 章节**（`report_section_builder.py:58-74`）
`事件概述 / 严重级别 / 风险评分 / 涉及账号 / 涉及资产 / 涉及进程 / 涉及文件 / 涉及外部地址 / 证据链 / 攻击故事线 / 攻击映射（ATT&CK）/ 已执行处置 / 验证结果 / 处置建议 / 附录索引`。

### 各功能之间的联系（怎么串成一条流水线）

**一句话**：`数据进 → 编排 → 取证 → 知识 → 图谱 → 评分 → 处置 → 验证 → 写回 → 报告 → 实时展示`，首尾还带一个"知识回流"闭环。

1. **事件类型 → 调查模板**：新事件按类型（8 类之一）匹配分诊模板、取证计划与处置预案，决定后面用哪些查询工具。
2. **查询工具 → 证据**：EvidenceAgent 并发调用查询工具取证，聚合出证据链；证据是后续所有环节的输入。
3. **证据 → 图谱**：GraphAgent 把证据里的六类实体抽成关系图、还原攻击路径，供风险评分使用。
4. **证据 → 知识增强**：RAGAgent 用证据摘要去检索知识库（ATT&CK/剧本/案例），命中结果带引用，供研判和报告。
5. **知识 + 图谱 + 证据 → 评分**：RiskAgent 用六维模型评分并给出 FinalVerdict 判定，证据置信度、攻击阶段、威胁情报都是评分输入。
6. **评分 → 处置方案**：RiskAgent 的结果决定处置计划范围，ResponseAgent 按能力清单生成 L0–L5 分级动作。
7. **处置 → 审批 → 执行**：分级动作经审批引擎判自动/人工，批准的处置由处置工具执行。
8. **执行 → 验证 → 写回**：处置后 VerifyAgent 用验证工具回查是否生效，通过后终态写回 XDR；失败走重规划，误处置走回滚工具撤销。
9. **全程 → 审计 → 报告**：状态机、决策轨迹、工具调用全程记审计，ReportAgent 汇总成 15 章节报告。
10. **实时呈现**：REST API + Socket.IO 把上述每一步状态实时推给前端，人工只看审批与异常即可。
11. **知识回流闭环**：案件结束后 MemoryAgent 把新案例/误报规律经人工审核沉淀回知识库，增强下一次 RAG 检索——**越用越聪明**。

**几个直接上 PPT 的解释（避免只列枚举名）**

- **事件生命周期 14 态**（`EventStatus`，`models/enums.py:14-36`）：`NEW 新建 → TRIAGING 分诊 → COLLECTING_EVIDENCE 取证 → ANALYZING 分析 → SCORING 评分 → PLANNING_RESPONSE 出方案 → WAITING_APPROVAL 待审批 → EXECUTING_RESPONSE 执行 → VERIFYING 验证 →（不满意回）REPLANNING 重规划`，终态为 `CONTAINED 已遏制（部分成功）`、`FAILED 失败`，最后经 `REPORTING 出报告 → CLOSED 关闭`。全程可追溯。
- **处置等级 L0–L5**（`ActionLevel`）：按影响半径分级——L1 通知/建单（低风险）→ L2 网络封禁（封 IP/域名）→ L3 主机/文件处置（隔离、隔离文件、杀毒、禁账号）→ L4 凭据处置（改密、吊销令牌）→ L5 保留。**L0/L1 自动审批，L2 及以上一律人工审批**（`workflow.py:73`，ISSUE-147 起收紧，杜绝 fail-open），30 分钟超时自动拒绝。
- **风险分级 Severity 4 级**：`low(0-39) / medium(40-69) / high(70-89) / critical(90-100)`，对应分数区间。
- **判定标签 FinalVerdict 四态**：`confirmed_threat 确认威胁 / false_positive 误报 / possible_false_positive 疑似误报 / none 未判定`，与事件状态相互独立，是"结论标签"而非"编排状态"。

---

## 第 6 页 · 创新点总览

> 页面标注：「基础任务全部完成，创新任务完成 xx 项」（xx 按实际完成数填）。

**创新点清单（每条可扩成 1 个小圆点/图标）**
1. **多 Agent 自主调查闭环** —— 12 Agent 协作，LangGraph 19 节点状态机 + REPLANNING 重规划。
2. **决策可解释可回溯** —— decision_trace 统一时间线（8 类条目），Agent 强制产出结构化 decision_basis，思维链与原始 payload 只存哈希、绝不落库。
3. **知识增强研判（RAG）** —— pgvector 向量 + 全文检索双路混合召回 → RRF 融合 → 重排 → 引用追踪，内置 78 条 ATT&CK 技术 + 13 剧本 + 12 误报/16 历史案例库。
4. **实体关系图 + 攻击路径 + STIX 2.1** —— 6 类实体、8 种关系，时间单调攻击链还原 + 跨事件横向移动，STIX 标准导出互通外部情报体系。
5. **六维风险评分 + 误报识别** —— LLM/规则 0.6:0.4 双路融合，温度校准防过信，双阈值区分真实威胁/误报。
6. **处置闭环与可靠写回** —— L0–L5 分级审批，outbox 幂等可靠投递 + readback 强证据 CLOSED 门 + Saga 回滚补偿。
7. **工作记忆字段归属** —— 45 个上下文字段绑定唯一受信 writer，CAS 乐观锁防越权污染。
8. **收敛护栏 + 预算** —— 80 步 / 30 次 LLM / 3 次重复 / 2 次振荡护栏 + 100 万系统 token 预算，防止多智能体跑飞。
9. **四维输出护栏 + 质量评分** —— schema/grounding/policy/sanitization 四维校验 + 四指标质量评分（0.75 通过线）。
10. **知识治理闭环** —— 权威知识 release 发布（STIX 校验 + 原子激活）+ 生成式记忆人工 review/promote/reject。
11. **工具调用 / 事件状态审计** —— 全链路审计日志可追溯。
12. **一键演示** —— `make up-demo && make demo-full-loop` 全栈闭环脚本。

**任务完成情况对照（PPT 上标"基础任务全部完成 / 创新任务完成 xx 项"）**

| 赛题任务 | 完成情况 | 对应能力 |
|---|---|---|
| 基础任务 1：安全运营智能体（告警自动研判等） | ✅ 完成 | 8 类事件 + 12 Agent 自动闭环 |
| 基础任务 2：安全知识库增强 + 工具扩展 | ✅ 完成 | 78 ATT&CK + 13 剧本 + RAG 流水线 + 可插拔 ToolProvider |
| 挑战任务：SuperAgent 闭环（多源协同 + ReAct + 全自动处置） | ✅ 完成 | LangGraph 编排 + 两阶段验证 + 写回闭环 |
| 创新任务（12 条创新点） | 完成 **N** 项（按实际完成填） | 决策可解释 / STIX 导出 / 误报识别 / 知识治理闭环 / 收敛护栏等 |

**每个创新点对应的"可演示证据"（评委要演示时不慌）**

| 创新点 | 一键演示/验证方式 |
|---|---|
| 多 Agent 闭环 | `make up-demo && make demo-full-loop`（全栈 CLOSED 金路径）；`EVAL_REQUIRE_CLOSED=1 make eval-full-loop`（严格门禁） |
| 决策可解释 | 浏览器打开事件 → 决策轨迹/审计日志页；`GET /events/{id}/decision-trace`、`/traces`、`/tool-calls` |
| 知识增强 RAG | 事件报告"攻击映射（ATT&CK）"章节展示命中技术与引用；`GET /knowledge` |
| 实体图/攻击路径/STIX | 事件关系图页；`GET /events/{id}/graph`、`/timeline`；STIX bundle 导入导出演示 |
| 风险评分/误报识别 | 事件详情页看六维评分与 FinalVerdict；误报场景跑 `fp_case_kb` 命中演示 |
| 处置闭环/写回 | 审批流演示（L2 动作人工审批）+ 写回状态页（readback CONFIRMED） |
| 知识治理 | 后台知识审核页 promote/reject 案例演示 |

---

## 第 7–11 页 · 五项关键技术

### 技术一（第 7 页）多智能体编排与可解释决策

**金句**：编排可信、决策可解释、行为可审计。

- **LangGraph 19 节点状态机**（`orchestration/workflow_graph.py:136-154`）：`triage → planner → evidence → fp_adjudication → graph → rag → risk → response → approval → approval_wait → execute → verify → report → close` 主链；节点列表见附录速查。含 `REPLANNING` 重规划环（`MAX_REPLAN_COUNT=3` 超限自动升级人工，`replan_handler.py`）、`writeback_recovery` 写回恢复节点、`halt` 停机节点。审批点/人工挂起点用 checkpoint 断点挂起（`interrupt_before`），可恢复续跑。
- **可回溯决策链**（`decision_trace_service.py`）：8 类带时间戳条目（Agent 执行 / 工具调用 / LLM 调用 / 状态迁移 / 审批 / 动作执行 / 处置 / 写回）合成一条时间线，含 active/total 时长（扣除等待）。
- **脱敏结构化依据**（`agent_trace_service.py`）：Agent 输出强制产出 `decision_basis`（输入摘要 / 证据引用 / 规则 / 模型 / 结论 / 置信度）；思维链（CoT）与原始 payload 只存 SHA256 引用、**永不落库**——这是"可解释但不泄密"的关键设计。
- **工作记忆字段归属**（`working_memory.py`）：45 个 `EventContext` 字段 → 唯一受信 writer 的 `FIELD_OWNERSHIP` 映射，非 owner 写入抛 `working_memory_unauthorized_write`；CAS 乐观锁防并发污染；scratchpad 200 条 FIFO + 访问日志。
- **收敛护栏**（`convergence_guard.py`）：`GLOBAL_MAX_STEPS=80`、`MAX_TOTAL_LLM_CALLS=30`、`MAX_DUPLICATE_TOOL_CALLS=3`、`MAX_OSCILLATION=2`，首个命中即强制收敛——防止多智能体无限循环/重复调用。
- **预算控制**（`budget_service.py`）：系统 100 万 token / 单事件 10 万 token / 单事件 5 美元 / 单 Agent 2 万 token，超限抛 `BudgetExceededError`，形成"预算耗尽"报告，不伪造成功。事件预算按严重度放大 0.5–2.0 倍（低危少花、危急多查）。
- **四维输出护栏**（`core/guardrails.py`）：schema 输出必须符合强类型模型 / grounding 引用证据必须真实存在 / policy 处置目标必须在实体集内 / sanitization 防密钥 PII 泄漏；出站处置护栏 fail-closed 永不降级。

---

### 技术二（第 8 页）知识增强研判（RAG + 安全知识库）

**金句**：检索可控、证据可溯、知识可信。

- **五步 RAG 流水线**（`backend/app/rag/pipeline.py`）：查询改写 → 计划校验 → 混合检索 → RRF 融合 → 重排 → 引用生成。
- **双路混合召回**（`hybrid_retriever.py` + `knowledge_store.py`）：pgvector **余弦向量检索** + PostgreSQL **全文检索**（`ts_rank`，0.5 分数下限）并行召回——向量抓语义、全文抓关键词，互为补充。
- **RRF 融合**（`rrf_fusion.py`，k=60）合并双路排名；**重排**（`reranker.py`，0.6 原始分 + 0.4 词重叠）精排；**查询改写**（`query_rewriter.py`，LLM 生成 ≤2 变体）扩展检索面；**引用追踪**（`citation_tracer.py`，≤200 字摘录 + 确定性 citation id）让每条知识可回原。
- **知识库规模**：
  - ATT&CK 技术库：**78 条技术 / 14 个战术**（attack_version v15.1），含 T1567/T1048/T1041 等数据外泄锚点。
  - Playbook 剧本库：**13 个 SOAR 剧本**。
  - 误报案例库 12 条 + 历史案例库 16 条。
- **知识治理闭环（两层）**：① 权威知识（ATT&CK/Playbook）STIX/JSON bundle **stage → 校验 → advisory-lock 原子激活 → 退役**（`knowledge_release_service.py`），幂等发布不重复；② 生成式记忆（历史案例/误报规则）人工 **review / promote / reject**（`memory_governance.py`，置信度 <0.5 且 pending 30 天自动拒绝）。
- **可量化阈值**：top_k=5、候选上限 50、ATT&CK 命中阈值 score≥0.3、误报相似度≥0.7 触发 FP 标记——检索是"打分制"而非黑盒。

---

### 技术三（第 9 页）实体关系图与攻击路径重构（含 STIX 导出）

**金句**：把杂乱多源告警沉淀成「有 ID、有边、有时间序、可导出 STIX」的攻防知识图谱。

- **六类实体**（`models/entities.py`）：账号 / 主机 / IP / 域名 / 进程 / 文件，IP 带内外网 scope 驱动处置目标。
- **8 种有向关系**：`logged_in_from 登录` / `logged_in_to` / `executed 执行` / `accessed 访问` / `connected_to 连接` / `resolved 解析` / `requested 请求` / `uploaded_to 上传`——每种关系由对应证据源规则自动抽取。
- **确定性 ID**：节点/边由 SHA-256 前 8 位派生，重复建图幂等去重——同一证据反复跑图结果一致。
- **攻击路径还原**（`graph_projection.py`）：时间单调 DFS（保证路径时序合理），单事件最长 **6 跳 / 最多 3 条** 攻击链；度中心 top-3 中心实体。
- **跨事件横向移动**（`attack_path_service.py`）：Neo4j Cypher 挖共享实体 + 横向移动链（同一账号/进程触碰多个主机，<2 主机判非横向）；Neo4j 未同步时 PostgreSQL 兜底，但绝不把报错伪装成无数据。
- **STIX 2.1 导出**（`stix_bundle_builder.py` + `stix_bundle_validator.py`）：ATT&CK 语料转标准 `attack-pattern` + `relationship`，UUIDv5 确定性 ID + 内容哈希 + 引用完整性校验——可被 SIEM/SOAR/TIP 直接消费，与外部情报/监管体系互通。
- **三层归一化去重**（`entity_merge.py` + `entity_validator.py`）：source > LLM > regex 优先级合并，上下文感知主机名校验（拒绝自然语言短语误判为主机名），主槽位竞争解决同名冲突。

---

### 技术四（第 10 页）风险评分与误报识别

**金句**：一致的风险分 + 防过信校准 + 双阈值误报判定。

- **六维加权模型**（`risk_scoring_engine.py:33-40`）：

  | 维度 | 含义 | 权重 |
  |---|---|---|
  | 资产影响 | 被攻击资产的价值等级 | 0.25 |
  | 行为异常 | 偏离正常基线的程度 | 0.20 |
  | 证据置信度 | 证据采集的充分性 | 0.20 |
  | 攻击阶段 | 处于 ATT&CK kill chain 的位置 | 0.15 |
  | 数据敏感度 | 涉及数据密级 | 0.10 |
  | 威胁情报 | 外部情报佐证 | 0.10 |

  `risk_score = 各维度 raw_score(0-100) × 权重 求和`，区间 0–100。
- **LLM / 规则双路融合**（`risk_agent.py`）：0.6 LLM + 0.4 规则逐维融合，LLM 不可用/降级自动退化为纯规则——**LLM 只是增强，不独占评分**。
- **防过信校准**（`confidence_calibration.py`）：temperature=1.2 压低置信度（校准后严格低于原始值）；证据稀疏时 confidence **封顶 0.35**，只降不升。
- **四态判定**（`verdict_resolver.py`）：`confirmed_threat / false_positive / possible_false_positive / none`，固定优先级：误报裁决建议 → 疑似误报(≥0.7) → 确认威胁(≥70 分) → 未判定。
- **误报双阈值**：向量预筛 ≥0.7 标记可疑（仅建议）；变更窗口类型化裁决分数 ≥0.88 且需 IDENTITY 授权证据才可 `close_as_fp`（`fp_adjudication_service.py`）——**误报关闭要"证据授权"，不能只凭"没恶意"**。
- **severity 分级**：≥90 CRITICAL / ≥70 HIGH / ≥40 MEDIUM / 其余 LOW。

---

### 技术五（第 11 页）处置闭环与可靠写回

**金句**：执行→独立验证→强证据 CLOSED→回滚补偿，每次写回可审计、可回退、可追责。

- **L0–L5 分级处置**（`models/enums.py`）：L1 通知/工单 → L2 封 IP/域名 → L3 隔离主机/隔离文件/杀毒/禁账号 → L4 改密/吊销令牌 → L5 保留；**L0/L1 自动、L2+ 一律人工审批**（ISSUE-147 收紧，杜绝 fail-open）。
- **审批引擎**（`approval_engine.py`）：硬门（工具必须在 provider 能力清单 / 写回就绪度合格 / 幂等性可用）+ 分级规则，30 分钟超时自动拒绝。
- **可靠投递 outbox**（`disposition_sync_service.py`）：命令白名单装配（绝不带自由参数/原始结果）→ 投递前审批 TOCTOU 复审 → FOR UPDATE 行锁 → 序号幂等 → payload 哈希 → `READY→LEASED→DELIVERED` 状态机；回执带序列号与证据等级。
- **writeback_readiness 8 态门控**（`writeback_readiness_resolver.py`）：提交前判定来源对象是否确定 / 适配器是否配置 / 写回能力是否支持 / 权限凭据是否齐全 / 连接器是否在线——**能力默认 UNKNOWN，只有正式文档确认才可 READY**，绝不猜厂商操作。
- **两阶段验证**（`verify_agent.py`）：Phase1 核验 IMMEDIATE 动作效果（隔离是否生效、封禁是否到位）；Phase2 才激活 deferred 终态 `EVENT_STATUS_UPDATE` 并核验写回——先验证效果、再写回状态。
- **CLOSED 强证据门**（`workflow.py validate_closed_gate`，ISSUE-302/362）：进入 CLOSED 需报告存在 + required 事件全部 applicable 动作写回 CONFIRMED + 终端回执 `confirmation_evidence ∈ {readback_verified, manual_confirmed}`，非 mock 环境还要求 `simulated=false`——**弱证据一律拒绝**。
- **回滚与补偿**（`rollback_service.py`）：**回滚≠删除**，回滚效果独立验证通过后才把原动作 CAS 为 `ROLLED_BACK`；跨动作 Saga 逆序补偿，生成 `COMPENSATION_RECORD`。

---

## 附录 A · 状态机 / 枚举 / 常量速查表（把"变量名"翻译成人话）

### A1. 调查状态机 19 个节点（`workflow_graph.py:136-154`）
`triage_node 分诊` → `begin_disposition_only_node`（仅处置模式）/ `manual_hold_node 人工挂起` / `close_node 关闭` 分支；主链 `planner_node 出计划 → evidence_node 取证 → fp_adjudication_node 误报裁决 → graph_node 图谱 → rag_node 知识增强 → risk_node 评分 → response_node 出方案 → approval_node 审批 → approval_wait_node 等待审批 → execute_node 执行 → verify_node 验证`；后续 `report_node 出报告 / replan_node 重规划 / writeback_recovery_node 写回恢复 / halt_node 停机`。

### A2. Agent 状态 5 态（`AgentStatus`）
`idle 空闲 / processing 处理中 / completed 完成 / failed 失败 / degraded 降级`。

### A3. 动作状态 11 态（`ActionStatus`）
`pending 待处理 → waiting_approval 待审批 → approved 已批准 → executing 执行中 → success 成功 / partial_success 部分成功 / failed 失败 / unknown 结果未知 / rolled_back 已回滚 / rejected 已拒绝 / superseded 已被新方案取代`。`unknown` 禁止盲重试，只能查证或人工裁决。

### A4. 证据来源 8 类（`EvidenceSource`）
`identity 身份（登录/账号行为） / endpoint 终端（进程/EDR） / data_security 数据安全（文件访问/数据外泄） / network_flow 网络流量 / dns DNS 解析 / asset 资产 / threat_intel 威胁情报 / false_positive_match 误报特征匹配`——每类对应一组查询工具，是取证与评分的输入分类。

### A5. 工具分类 4 类（`ToolCategory`）
`query 查询取证（只读，不产生处置动作） / response 处置（产生外部副作用） / verification 验证（只读核验） / rollback 回滚（补偿动作）`。

### A6. 写回就绪度 8 态（`WritebackReadiness`，提交前门控，非外部回执）
`not_required 不要求写回 / ready 就绪 / source_unresolved 写回来源对象未确定 / not_configured 适配器未配置 / capability_unknown 写回能力未知（默认态，需正式文档确认）/ capability_unsupported 能力明确不支持 / permission_denied 无权限凭据 / connector_unavailable 连接器离线`。

### A7. 投递队列 6 态（`OutboxDeliveryStatus`，本地队列状态，不冒充外部事实）
`ready 待投递 → leased 被 worker 租约（30s）→ delivered 已投递 / waiting_retry 等待重试（指数退避）/ paused 暂停（结果未知，转 lookup 查证）/ dead_letter 死信（确定性失败，转人工）`。

### A8. 写回状态 8 值（`WritebackStatus`，外部回执状态）
`pending / sending / accepted 外部已接收 / confirmed 回读确认成功（强证据）/ partial 部分 / failed / conflict 版本冲突 / unknown 结果未知`。**accepted ≠ confirmed**：接收不代表成功，只有回读验证才算。

### A9. 证据强度 4 级（`ConfirmationEvidence`，由弱到强）
`adapter_acknowledged 仅回执已收 < status_queried 状态查询 < readback_verified 回读验证 < manual_confirmed 人工确认`。CLOSED 门只认后两者（`workflow.py:545-550`）。

### A10. 预算常量（`workflow.py:92-113`）
`GLOBAL_TOKEN_BUDGET=100 万 token`（系统级总量）/ `EVENT_TOKEN_BUDGET=10 万`（单事件，按 severity 0.5/1.0/1.5/2.0 倍放权）/ `EVENT_COST_BUDGET_USD=5 美元`（单事件成本上限）/ `PER_AGENT_TOKEN_CAP=2 万`（单 Agent 调用上限）/ 收敛护栏 `80 步 / 30 次 LLM / 3 次重复工具 / 2 次振荡`。

### A11. 其它关键常量
`MAX_REPLAN_COUNT=3`（重规划上限，超限升级人工）/ `MAX_AGENT_RETRIES=2` / `CONFIDENCE_THRESHOLD=0.7` / `FP_LOW=0.7, FP_HIGH=0.9` / `APPROVAL_TIMEOUT=30 分钟` / 证据采集全局超时 30s、单源 10s / `MIN_EVIDENCE_SOURCES=3`。

---

## 附录 B · 可靠性设计铁律（从上游 issue/PR 记录提炼，PPT 强"可信"卖点）

这些不是口号，是代码层面强制落地的约束（对应 commit 与 `.planning/` 审计 issue）：

1. **accepted ≠ confirmed**：外部"已接收"绝不等于"已成功"，只有 readback 回读验证后才能判成功（`.planning/2026-08-11-pre-release-audit/gh-issue-311.md`）。Mock 回执必须标 `simulated=true`，UI 不得把模拟与真实画成同级绿。
2. **禁止本地成功冒充外部已回写**：分析内容（研判正文/报告/prompt/思维链）**永不写回 XDR**；只有经策略校验和审批的处置动作可写回，且只写白名单字段。
3. **确定性错误分类**：`not_found` 等明确 rejection 进死信/终态，**不无限重试**；只有传输类故障（结果真未知）才 pause + lookup 查证（`.planning/2026-08-10-dynamic-eval-audit/gh-issue-300.md`）。
4. **能力默认 UNKNOWN**：不知道厂商是否支持某种操作时，一律视为不支持/需人工，绝不凭名称或截图猜操作；capability 只有正式接口文档确认才可 READY。
5. **回滚≠删除**：回滚创建独立 rollback 动作，需独立验证通过后才把原动作 CAS 为已回滚；`unknown/partial_success` 永不自动回滚（`rollback_service.py`）。
6. **思维链与原始 payload 永不落库**：只存 SHA256 引用，CoT 键明确置 `[NOT_RETAINED]`（`agent_trace_service.py`）。
7. **两阶段验证**：先核验动作效果（IMMEDIATE），再激活终态状态写回（POST_VERIFY），终态 `EVENT_STATUS_UPDATE` 必须绑定当前 plan_revision 与 closure_cycle。
8. **CLOSED 是强证据门**：弱证据（adapter_acknowledged / status_queried）一律拒绝进入 CLOSED（ISSUE-302/362）。

---

## 附录 C · 工程可信度与可演示性（评委问"做了多少/怎么跑"用）

**工程规模（可直接上 PPT 的数字）**
- 测试：**349 个测试文件 / 约 17 万行测试代码**；CI 含契约基线校验（`make test-ci-lite` / `make check-contract-drift`）。
- 迭代：仓库 git 累计 **1196 次提交、122 个已合并 PR**（上游为主，本 fork 持续跟进）。
- 契约驱动：OpenAPI / JSON Schema / Socket.IO 事件 Schema 三方契约，改动自动 `check-contract-drift` 防漂移。

**6 个演示场景**（`data/scenarios/` + `data/mock/`）
`insider_data_exfiltration 张三内鬼数据外泄`（主演示）、`host_compromise 主机失陷`、`insider_privilege_abuse 内部特权滥用`、`lateral_movement 横向移动`、`malicious_process 恶意进程`、`other_unclassified 未分类`。

**6 套评测矩阵**（`data/evaluation/`，对 12–17 页性能页也可复用）
`agentic_coordination_v1（16 用例，多 Agent 协调/越权/隔离/幂等）`、`detection_shadow_v1（5 用例，检测基线）`、`detection_shadow_v1_fail_closed（2 用例）`、`security_knowledge_v1（11 用例，RAG 引用/租户过滤）`、`shadowtrace_demo_v1（3 用例）`、`detection_production_v1`。

**一键演示命令（Makefile）**
- 全栈闭环：`make up-demo && make demo-full-loop`
- 分步金路径：`make up-demo && make bootstrap-demo-full-loop && EVAL_REQUIRE_CLOSED=1 make eval-full-loop`
- 分析种子 + 冒烟：`make up-demo && make bootstrap-demo && make smoke-demo`
- 动态评测矩阵：`make eval-full-loop-matrix`

---

## 附录 D · 痛点 → 方案（第 2–3 页背景页可参考）

| 痛点（赛题背景） | ShadowTrace 方案 |
|---|---|
| 攻击者已用大模型实现自动化（个性化钓鱼、自动挖洞），传统安全工具 + 单一小模型难以应对 | 多 Agent 协同编排 + 78 条 ATT&CK 知识增强研判，覆盖告警→闭环处置 |
| 安全告警量巨大、研判严重依赖人工、处置闭环慢 | 自动分诊→取证→评分→处置→验证全链路；L0/L1 自动审批，人工只处理高影响动作 |
| 安全运营工具碎片化，缺少统一编排与可解释追溯 | LangGraph 统一状态机 + decision_trace/审计日志全链路可解释、可回溯 |
| 大模型输出不可信、易过信、易越权 | 双路评分（LLM/规则 0.6:0.4）+ 温度校准 + 四维输出护栏 + 工作记忆字段归属 |

---

## 附 · 给 khw 制作时的一个一致性提醒

图关系：`GraphRelationType`（`agent_io.py:116-124`）定义了 8 种关系，但同步到 Neo4j 的过滤集合 `graph_sync_service.py` 的 `_RELATION_TYPES` 与它只有 3 个重叠。若 PPT 声称「8 种关系全部镜像到 Neo4j」，建议先与工程侧确认该过滤集合是否要更新（不影响 PostgreSQL 主路径的 8 种关系）。

---

*素材来源：仓库 README.md + backend/app 源码 + .planning/ 上游 issue 审计记录 + git 历史 + 赛题 PDF（XH-202614）；各节关键文件路径与 issue 编号已标注，需要贴图/深挖时按路径定位即可。*


